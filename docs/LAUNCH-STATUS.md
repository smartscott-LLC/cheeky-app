# Launch Status & Remaining Work

**Updated:** 2026-08-06 · **Target:** launch Friday (pushed from Saturday — 48-72h window)

Everything below is verified against the repo, the hosted DB, and the live site.
Three buckets: **✅ Done** (locked, not changing), **⏳ Before Friday**, **🧭 Later/deferred**.

---

## ✅ DONE — the club is built and verified

| Area | State |
|---|---|
| The app (all rooms) | Landing, lobby, 4 floors, Event Center, crew chats, Gift Shop, Coat Check, Swag Shop, Spark List, Cheeky Chats, Account, The Exchange — live |
| Money rail | Stripe subscriptions + token packs, server-side token ledger, webhook idempotency (replays can't double-grant) |
| Verification | One-stop door: consents + account with Brutus → Stripe Identity → lobby. ID/selfie never stored |
| Events | Hourly playlist intact — **:00 Dance Floor, :15 Themed Night, :30 Speed Dating, :45 Rooftop** — server-generated by cron, all four confirmed in the migration |
| Token engine | Ledger + holds model; advisory-lock join; **stress-tested 1000 concurrent joins, all consistent** (commit `55850ec`) |
| Safety | DateSafe AI image review + human desk, report/block everywhere, honeypots + bot activity guard, **ban registry enforced at signup AND sign-in** |
| AI staff | 6 characters, floor-gated chat, rate-limited endpoint (60/hr member + 200/hr IP — commit `36b39ef`) |
| Lions Den | Mint (presets + bundles), announcements, model failover, floor closures, metrics, safety desk, bans, kill-switch |
| Email | Resend wired — welcome / apology / ban notices (smoke-tested live) |
| SEO | Sitemap page + `sitemap.xml` + robots.txt; Search Console submission queued at the **www** URL |
| Performance | **Lighthouse 99 / 98 / 100 / 100** (fresh audit run); WebP pass (−40MB served), lazy Stripe, SW fixed, PostHog removed |
| Docs & repo | Changelog, CONTRIBUTING, component library, env reference, governance, archives — aligned |
| Test suite | Unit + webhook + token-engine (1000-burst) + AI probe; CI runs lint/test/build |

---

## ⏳ BEFORE FRIDAY — the short list

1. **Event-kind + finalize test coverage** *(the one real code gap — I build this)*
   - Automated tests for **Speed Dating** mechanics (rotation/certificates), **Themed Night**, **Rooftop**, the **Date Night** game, and a **`finalize_events` cycle under load** (the minute-cron releasing holds at volume was never stress-tested — the 1000-burst tested joins, not finalize).
   - Same throwaway-members pattern as the existing burst; runnable anytime with `RUN_LIVE_TESTS=1`.
2. **Friday-morning ops checks** *(your dashboard, ~20 min)*
   - [ ] Sentry: confirm errors are actually flowing (one misconfigured env var = silent black hole)
   - [ ] Stripe dashboard: use the **replay button** on a verification webhook — confirm no double token grant
   - [ ] One manual full event cycle: sign up → join → watch open → running → closed → no-match refund
   - [ ] Vercel deployment logs handy for the morning
3. **Phone install path decision** *(small, need your call)*
   - The app **is already an installable PWA** (manifest + service worker). If we're **not** going through Google Play, then the Android wrapper extras (keystore, `assetlinks.json` fingerprint) become unnecessary — the path is simply **"Add to Home Screen"** on Android + iOS. I'll add a small **install prompt** (one component) and we test on a real phone. Confirm and I'll set it up.
4. **Search Console** — confirm the **www** sitemap submission went through (your dashboard).

---

## 🧭 LATER / DEFERRED (deliberately parked)

- **A11y 96→100 nitpicks** (labels, focus rings, semantics) — deferred by founder's call; the pink/white JOIN NOW button stays as-is (brand over checkbox; it still reads as "right there at 100").
- **Rate-limit edge cases** (window-boundary bursts) — non-exploitable at current scale; noted in the audit.
- **Profile-update rate limits** (bio/photo) — flagged in the audit; catch it in safety metrics post-launch.
- **Banned-account registry UI in the suite** — the guard is built and live; automated test coverage comes with the events pass above.
- **Marketing games + the hurdles you found** — parked until this status doc is locked; then we design them properly (Den mint makes giveaways a 2-minute job).

---

## The one sentence

The product is launch-ready with zero blockers; the only code work before Friday is **test coverage for the four event kinds + finalize under load**, and everything else is a short list of dashboard confirmations. Nothing is missing that would make launch unsafe — and anything we find Friday, we fix Friday, because the suite + git history are our net.
